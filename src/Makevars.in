# src/Makevars.in -> src/Makevars
# Generated by configure script for Unix-like systems.

# Flags detected by configure using pkg-config for pre-installed grpc++ library
PKG_CXXFLAGS = @GRPC_PKG_CXXFLAGS@
PKG_LIBS = @GRPC_PKG_LIBS@

# C++ Standard determined by configure (should be C++17)
CXX_STD = CXX17

# Optional: Add other flags if needed, but usually pkg-config handles it.
# Example: PKG_CPPFLAGS = -DMY_DEFINE
